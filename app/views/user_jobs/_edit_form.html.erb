<%= form_for(@user_job) do |f| %>

	<% if @user_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_job.errors.count, "error") %> prohibited this Comment from being saved:</h2>
      <ul>
      <% @user_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_comment %><br>
    <%= f.text_field :user_comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
